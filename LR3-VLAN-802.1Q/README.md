# Лабораторная работа 3 — «Логические границы»

**Аннотация:** в этой лабораторной работе студент изучает VLAN, access- и trunk-порты, а также тегирование кадров по стандарту 802.1Q на основе анализа готового захвата трафика с trunk-соединения. Работа выполняется без Open vSwitch, без GNS3 VM и без дополнительных виртуальных машин: обязательная часть лабораторной сводится к анализу предоставленного `pcapng` в Wireshark.

---

## Содержание

1. [Chapter I](#chapter-i) \
   1.1. [Рекомендации к работе](#рекомендации-к-работе)
2. [Chapter II](#chapter-ii) \
   2.1. [Теоретические основы лабораторной работы](#теоретические-основы-лабораторной-работы) \
   2.2. [Общие сведения о формате выполнения и опорной топологии](#общие-сведения-о-формате-выполнения-и-опорной-топологии) \
   2.3. [Общие сведения о Wireshark](#общие-сведения-о-wireshark) \
   2.4. [Полезные материалы](#полезные-материалы)
3. [Chapter III](#chapter-iii) \
   3.1. [Общие требования к отчётам](#общие-требования-к-отчётам)
4. [Chapter IV](#chapter-iv) \
   4.1. [Задание 1. Определение варианта и изучение схемы](#задание-1-определение-варианта-и-изучение-схемы) \
   4.2. [Задание 2. Детальный анализ последовательности по варианту](#задание-2-детальный-анализ-последовательности-по-варианту) \
   4.3. [Задание 3. Анализ второй VLAN и сравнительный разбор](#задание-3-анализ-второй-vlan-и-сравнительный-разбор) \
   4.4. [Задание 4. Итоговая аналитическая часть](#задание-4-итоговая-аналитическая-часть)

### Введение

>Когда в сети появляется несколько групп узлов, одной только физической схемы уже недостаточно. Нужно не просто соединить устройства проводами, а разделить трафик по логическим границам, чтобы широковещательные домены не смешивались между собой. Именно эту задачу решают VLAN. В данной лабораторной работе предстоит не настраивать сложный программный коммутатор с нуля, а посмотреть на результат его работы через Wireshark и по реальным кадрам доказать, как устроены trunk, access и тег 802.1Q.

## Цель работы

Целью лабораторной работы является изучение логического разделения коммутируемой сети на VLAN, понимание различия между access- и trunk-портами, а также практическое подтверждение того, что по trunk-соединению кадры передаются с тегом 802.1Q.

В результате выполнения лабораторной работы студент должен уметь:

1. объяснять, как коммутатор обучается MAC-адресам;
2. описывать, что такое flooding и когда он возникает;
3. объяснять назначение VLAN;
4. различать access-порт и trunk-порт;
5. читать схему стенда с двумя коммутаторами и четырьмя хостами;
6. находить в Wireshark кадры разных VLAN;
7. определять по кадру наличие тега 802.1Q и VLAN ID;
8. оформлять аналитический отчёт по готовому захвату трафика.

## Chapter I

### Рекомендации к работе

Как работать с лабораторной:

- Выполняй работу последовательно: сначала изучи схему и адресный план, затем открой захват и только после этого переходи к анализу отдельных кадров.
- Сразу вычисли значения параметров `A`, `B`, `C` и `D` по номеру студента `N` и укажи их в начале отчёта.
- Обязательная часть лабораторной выполняется по готовому файлу `pcapng`; самостоятельная настройка Open vSwitch, GNS3 VM, Docker, VirtualBox и VMware не требуется.
- В этой лабораторной варианты не меняют сам файл захвата и не меняют адресный план: каждый параметр задаёт обязательный акцент внутри своего задания и влияет на содержание отчёта.
- Не анализируй захват «вслепую»: сначала отдели трафик `VLAN 10` и `VLAN 20` фильтрами, затем найди внутри каждой последовательности `ARP` и `ICMP`.
- Для каждого ключевого кадра фиксируй не только его имя, но и поля: `Source MAC`, `Destination MAC`, `Source IP`, `Destination IP`, `TPID`, `VLAN ID`.
- В выводе объясняй не только «что видно в Wireshark», но и «что это доказывает» с точки зрения VLAN и trunk.

Как оформлять результат:

- Отчёт должен читаться как цельный аналитический документ, а не как набор разрозненных скриншотов.
- На каждом скриншоте Wireshark должны быть видны список пакетов, дерево полей и байтовое представление.
- Термины `VLAN`, `access`, `trunk`, `802.1Q`, `flooding`, `broadcast domain`, `VLAN ID` следует использовать корректно и по смыслу.

## Chapter II

### Теоретические основы лабораторной работы

#### Обучение коммутатора по MAC-адресам

Коммутатор канального уровня строит таблицу соответствий `MAC → порт`, наблюдая адреса источника входящих кадров. Когда коммутатор получает кадр, он запоминает, с какого порта пришёл адрес источника, и заносит эту информацию в MAC-таблицу.

Если коммутатор получает кадр и ещё не знает, на каком порту находится адрес назначения, он выполняет flooding — рассылает кадр по всем допустимым портам данного сегмента, кроме входного. После получения ответа от адресата MAC-таблица пополняется, и следующие кадры передаются уже адресно, а не широковещательно.

В рамках данной лабораторной работы это особенно важно для понимания ARP-запроса: именно широковещательный `ARP Request` сначала попадает в нужную VLAN, а уже затем `ARP Reply` возвращается как одноадресный кадр.

#### Зачем нужны VLAN

VLAN (`Virtual Local Area Network`) позволяет разделить один физический коммутатор на несколько логических широковещательных доменов. Узлы внутри одной VLAN могут обмениваться кадрами на втором уровне, а узлы разных VLAN не могут взаимодействовать напрямую на L2 без маршрутизации.

VLAN используется для:

- логического разделения пользователей и сервисов;
- уменьшения области широковещания;
- повышения управляемости сети;
- упрощения сегментации трафика.

Если два хоста физически подключены к одной коммутируемой инфраструктуре, это ещё не означает, что они принадлежат одному логическому сегменту. Принадлежность к VLAN важнее, чем сама физическая близость.

#### Access-порт и trunk-порт

Access-порт принадлежит одной VLAN и обычно подключается к конечному устройству. Конечный узел на таком порту получает и отправляет нетегированные Ethernet-кадры, а принадлежность кадра к VLAN задаётся самим коммутатором на основании конфигурации порта.

Trunk-порт переносит трафик нескольких VLAN одновременно. Для этого между коммутаторами в кадр добавляется специальный тег IEEE 802.1Q. Благодаря этому один физический линк может служить сразу нескольким логическим сегментам.

Именно поэтому при анализе трафика на trunk-соединении студент видит не «обычные» Ethernet-кадры, а кадры с добавленным полем VLAN.

#### Стандарт 802.1Q

При передаче кадра по trunk-соединению между MAC-адресами и полем `EtherType` вставляется тег 802.1Q. В этом теге особенно важно поле `VID` (`VLAN ID`), которое и указывает, к какой VLAN относится данный кадр.

Структура тега 802.1Q:

| Поле | Размер | Назначение |
|---|---|---|
| `TPID` | 2 байта | идентификатор тега (`0x8100`) |
| `PCP` | 3 бита | приоритет трафика |
| `DEI` | 1 бит | индикатор допустимости отбрасывания |
| `VID` | 12 бит | идентификатор VLAN |

Для данной лабораторной работы ключевыми являются два наблюдения:

1. у trunk-кадра присутствует `TPID = 0x8100`;
2. внутри тега можно явно прочитать `VLAN ID`.

#### Что необходимо понять по итогам теории

После изучения теоретического материала студент должен понимать:

1. как коммутатор обучается MAC-адресам;
2. почему широковещательные кадры ограничиваются рамками своей VLAN;
3. чем access-порт отличается от trunk-порта;
4. зачем между коммутаторами нужен trunk;
5. где именно в кадре появляется тег 802.1Q.

### Общие сведения о формате выполнения и опорной топологии

#### Формат выполнения без дополнительных установок

В облегчённой версии практикума лабораторная работа выполняется по готовому захвату, полученному с преднастроенного стенда. Такой формат позволяет проверить именно понимание VLAN и 802.1Q, не превращая работу в настройку Open vSwitch, Linux-коммутаторов или виртуализации на хостовой ОС.

Для обязательной части лабораторной достаточно:

- `GNS3` — для чтения и понимания схем стендов курса;
- `Wireshark` — для анализа файла захвата.

Дополнительные компоненты, такие как `Open vSwitch`, `GNS3 VM`, `Docker`, `VirtualBox` и `VMware`, для данной лабораторной **не требуются**.

#### Опорная топология

Логика стенда, по которому подготовлен захват:

```text
PC1 ---- SW1 ----\
                  \ 
                   == trunk ==
                  / 
PC2 ---- SW1 ----/           \---- SW2 ---- PC3
                              
                              \---- SW2 ---- PC4
```

Более точно распределение узлов по VLAN выглядит так:

- `PC1` подключён к `SW1` как access-порт `VLAN 10`;
- `PC2` подключён к `SW1` как access-порт `VLAN 20`;
- `PC3` подключён к `SW2` как access-порт `VLAN 10`;
- `PC4` подключён к `SW2` как access-порт `VLAN 20`;
- линк `SW1 — SW2` работает как `trunk` и переносит `VLAN 10` и `VLAN 20`.

Захват сделан **на trunk-линке** между `SW1` и `SW2`.

#### Таблица адресации

Для всех студентов используется единый фиксированный адресный план:

| Узел | IP-адрес | Роль |
|---|---|---|
| `PC1` | `192.168.10.11/24` | хост VLAN 10 на стороне `SW1` |
| `PC3` | `192.168.10.33/24` | хост VLAN 10 на стороне `SW2` |
| `PC2` | `192.168.20.22/24` | хост VLAN 20 на стороне `SW1` |
| `PC4` | `192.168.20.44/24` | хост VLAN 20 на стороне `SW2` |

#### Используемый файл захвата

В каталоге лабораторной должен находиться файл:

- `pcap/lr3_vlan_trunk_demo.pcapng`

В этом захвате представлены две отдельные короткие последовательности:

1. `ARP` + `ICMP` для `VLAN 10`;
2. `ARP` + `ICMP` для `VLAN 20`.

### Общие сведения о Wireshark

В данной лабораторной работе Wireshark используется для анализа уже готового файла захвата. Полезно заранее подготовить следующие фильтры отображения:

| Фильтр | Назначение |
|---|---|
| `vlan` | показать только кадры с тегом 802.1Q |
| `vlan.id == 10` | показать только кадры VLAN 10 |
| `vlan.id == 20` | показать только кадры VLAN 20 |
| `arp` | показать только ARP-кадры |
| `icmp` | показать только ICMP-пакеты |
| `arp && vlan.id == 10` | найти ARP внутри VLAN 10 |
| `icmp && vlan.id == 20` | найти ICMP внутри VLAN 20 |

При анализе обязательно проверь:

1. наличие поля `802.1Q Virtual LAN`;
2. значение `TPID = 0x8100`;
3. значение `VLAN ID`;
4. MAC- и IP-адреса отправителя и получателя.

### Полезные материалы

В каталоге лабораторной должны находиться:

- `README.md` — основной текст лабораторной;
- `report.docx` — версия в формате Word;
- `report.pdf` — версия в формате PDF;
- `pcap/lr3_vlan_trunk_demo.pcapng` — готовый захват для анализа.

## Chapter III

### Общие требования к отчётам

Требования к отчётам, сформулированные в лабораторной работе № 1, применяются ко всем отчётам цикла. Ниже приведены дополнительные требования, специфичные для данной лабораторной работы.

В отчёте должны быть:

- титульный лист;
- цель работы;
- краткая теория по теме VLAN, trunk и 802.1Q;
- схема стенда;
- таблица адресации;
- таблица принадлежности хостов к VLAN;
- номер студента `N` и вычисленные значения параметров `A`, `B`, `C`, `D`;
- минимум 4 скриншота Wireshark для основной последовательности по варианту `A`;
- минимум 2 дополнительных скриншота Wireshark для второй VLAN;
- таблица анализа кадров с указанием номера кадра, MAC-адресов, IP-адресов, `VLAN ID` и протокола верхнего уровня;
- отдельный комментарий по варианту `B`;
- сравнительный комментарий по варианту `C`;
- итоговый аналитический комментарий по варианту `D`;
- краткий аналитический вывод.

Дополнительно необходимо учитывать следующее:

- на скриншоте trunk-кадра должны быть видны и список пакетов, и разбор поля `802.1Q`, и байтовое представление;
- в таблице анализа нужно явно указать, какая последовательность является основной по варианту `A`, а какая относится ко второй VLAN;
- в приложениях к отчёту должен присутствовать файл `pcap/lr3_vlan_trunk_demo.pcapng`.

## Chapter IV

### Задание 1. Определение варианта и изучение схемы

В данной лабораторной работе варианты не меняют исходный файл `pcap/lr3_vlan_trunk_demo.pcapng` и не меняют таблицу адресации. Вариант определяет, какая последовательность считается основной для детального разбора и на каком типе кадров студент делает основной акцент в отчёте.

Для этого задания используется параметр `A`, который вычисляется по формуле:

- `A = ((N - 1) mod 4) + 1`

Вариант `A`:

| `A` | Основная VLAN | Базовая пара узлов | Первый акцент в отчёте |
|---|---|---|---|
| `1` | `VLAN 10` | `PC1 → PC3` | `ARP Request` и `ARP Reply` |
| `2` | `VLAN 10` | `PC1 → PC3` | `ICMP Echo Request` и `ICMP Echo Reply` |
| `3` | `VLAN 20` | `PC2 → PC4` | `ARP Request` и `ARP Reply` |
| `4` | `VLAN 20` | `PC2 → PC4` | `ICMP Echo Request` и `ICMP Echo Reply` |

Изучи опорную схему стенда и таблицу адресации из методических указаний.

Необходимо зафиксировать в отчёте:

1. какие два хоста находятся в `VLAN 10`;
2. какие два хоста находятся в `VLAN 20`;
3. какой линк является trunk;
4. на каком именно линке выполнен захват;
5. значение параметра `A` и какая последовательность становится основной для дальнейшего разбора.

В отчёт по данному заданию включить:

- схему стенда;
- таблицу адресации;
- таблицу вида `узел → VLAN`;
- краткую фиксацию значения параметра `A`.

### Задание 2. Детальный анализ последовательности по варианту

Для этого задания используется параметр `B`, который вычисляется по формуле:

- `B = ((N - 1) mod 3) + 1`

Вариант `B`:

| `B` | Дополнительный аналитический акцент |
|---|---|
| `1` | объяснить переход от широковещательного `ARP Request` к одноадресным кадрам ответа и `ICMP` |
| `2` | подробно показать место тега `802.1Q` и пояснить роль полей `TPID` и `VLAN ID` |
| `3` | пояснить, какие кадры можно связать с flooding, а какие — с адресной пересылкой после обучения по MAC |

Открой файл `pcap/lr3_vlan_trunk_demo.pcapng` в Wireshark и выполни детальный анализ **основной** последовательности, определённой параметром `A`.

Порядок выполнения:

1. если по параметру `A` основной является `VLAN 10`, применить фильтр `vlan.id == 10`; если основной является `VLAN 20`, применить фильтр `vlan.id == 20`;
2. найти `ARP Request`;
3. найти `ARP Reply`;
4. найти `ICMP Echo Request`;
5. найти `ICMP Echo Reply`;
6. для каждого из четырёх кадров выписать:
   - номер кадра;
   - `Source MAC`;
   - `Destination MAC`;
   - `Source IP`;
   - `Destination IP`;
   - `TPID`;
   - `VLAN ID`;
   - протокол верхнего уровня;
7. отдельно дать письменный комментарий по варианту `B`.

В отчёт по данному заданию включить:

- не менее 4 скриншотов Wireshark для основной последовательности;
- подробную таблицу анализа четырёх кадров;
- отдельный комментарий по варианту `B`.

### Задание 3. Анализ второй VLAN и сравнительный разбор

Для этого задания используется параметр `C`, который вычисляется по формуле:

- `C = ((N - 1) mod 2) + 1`

Вариант `C`:

| `C` | Обязательный сравнительный комментарий |
|---|---|
| `1` | сравнить, как в двух VLAN сохраняется одна и та же логика `ARP` и `ICMP`, но меняются адреса и `VLAN ID` |
| `2` | сравнить, как один и тот же trunk переносит трафик двух VLAN, не смешивая логические сегменты |

После основного разбора перейди ко второй VLAN, то есть к той последовательности, которая **не** была выбрана основной по параметру `A`.

Порядок выполнения:

1. применить фильтр для второй VLAN (`vlan.id == 10` или `vlan.id == 20` в зависимости от значения `A`);
2. найти `ARP Request` и `ARP Reply`;
3. найти `ICMP Echo Request` и `ICMP Echo Reply`;
4. выписать те же поля, что и для основной последовательности;
5. подготовить сравнительную таблицу `основная VLAN ↔ вторая VLAN`;
6. дать письменный комментарий по варианту `C`.

В отчёт по данному заданию включить:

- не менее 2 скриншотов Wireshark для второй VLAN;
- сравнительную таблицу по двум VLAN;
- комментарий по варианту `C`.

### Задание 4. Итоговая аналитическая часть

Для этого задания используется параметр `D`, который вычисляется по формуле:

- `D = ((N - 1) mod 4) + 1`

Вариант аналитического акцента по параметру `D`:

| `D` | Обязательный расширенный комментарий |
|---|---|
| `1` | пояснить, почему один trunk переносит трафик нескольких VLAN, но не объединяет их в один широковещательный домен |
| `2` | пояснить, почему в захвате отсутствует межсетевой обмен между `VLAN 10` и `VLAN 20`, хотя внутри каждой VLAN есть связность |
| `3` | показать, как одинаковая логика `ARP` и `ICMP` повторяется в двух VLAN при изменении только адресов и `VLAN ID` |
| `4` | объяснить, почему именно захват на trunk-линке позволяет доказать и наличие тегирования `802.1Q`, и раздельность логических сегментов |

На основании захвата необходимо сделать выводы по следующим вопросам:

1. почему хосты внутри одной VLAN могут обмениваться кадрами через trunk;
2. почему трафик `VLAN 10` и `VLAN 20` не смешивается, хотя проходит по одному и тому же физическому линку;
3. почему отсутствие маршрутизатора не позволяет организовать прямой обмен между двумя VLAN на третьем уровне;
4. какие именно поля кадра в Wireshark являются наблюдаемым доказательством того, что trunk работает корректно.

Дополнительно необходимо выполнить аналитический пункт по варианту `D`.

Итоговый вывод лабораторной работы должен содержать четыре обязательные мысли:

- VLAN разделяет сеть на отдельные широковещательные домены;
- trunk позволяет перенести несколько VLAN по одному физическому линку;
- один физический линк может обслуживать несколько логических сегментов без смешивания их трафика;
- тег `802.1Q` является прямым наблюдаемым доказательством принадлежности кадра к конкретной VLAN.

В отчёт по данному заданию включить:

- письменные ответы на четыре обязательных вопроса;
- отдельный развёрнутый комментарий по варианту `D`;
- итоговый вывод лабораторной работы.
