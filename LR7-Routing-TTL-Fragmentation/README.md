# Лабораторная работа 7 — «Маршрут, который становится короче»

**Аннотация:** в этой лабораторной работе студент собирает многозвенную маршрутизируемую сеть из трёх маршрутизаторов, проверяет выбор маршрута по наиболее длинному префиксу, исследует уменьшение поля TTL и демонстрирует фрагментацию IP-пакетов, а также ошибку при установленном флаге DF.

---

## Содержание

1. [Chapter I](#chapter-i) \
   1.1. [Рекомендации к работе](#рекомендации-к-работе)
2. [Chapter II](#chapter-ii) \
   2.1. [Теоретические основы лабораторной работы](#теоретические-основы-лабораторной-работы) \
   2.2. [Общие сведения о GNS3 и Linux-узлах](#общие-сведения-о-gns3-и-linux-узлах) \
   2.3. [Общие сведения о Wireshark](#общие-сведения-о-wireshark) \
   2.4. [Полезные материалы](#полезные-материалы)
3. [Chapter III](#chapter-iii) \
   3.1. [Общие требования к отчётам](#общие-требования-к-отчётам)
4. [Chapter IV](#chapter-iv) \
   4.1. [Задание 1. Сборка стенда и настройка маршрутизации](#задание-1-сборка-стенда-и-настройка-маршрутизации) \
   4.2. [Задание 2. Проверка связности и таблицы маршрутов](#задание-2-проверка-связности-и-таблицы-маршрутов) \
   4.3. [Задание 3. Демонстрация longest prefix match](#задание-3-демонстрация-longest-prefix-match) \
   4.4. [Задание 4. Демонстрация TTL](#задание-4-демонстрация-ttl) \
   4.5. [Задание 5. Демонстрация фрагментации](#задание-5-демонстрация-фрагментации) \
   4.6. [Задание 6. Демонстрация DF и ICMP-ошибки](#задание-6-демонстрация-df-и-icmp-ошибки)

### Введение

>Когда пакет идёт через несколько маршрутизаторов, он каждый раз чуть-чуть меняется: TTL уменьшается, путь пересчитывается по таблице маршрутов, а иногда пакет вообще приходится дробить на части. На схеме это выглядит просто, но в реальном захвате всё становится заметно по конкретным полям заголовка. Эта лабораторная работа как раз и нужна для того, чтобы увидеть путь пакета в деталях.

## Цель работы

Целью лабораторной работы является закрепление принципов статической маршрутизации в многозвенной сети, а также практическое изучение поведения поля TTL, фрагментации IP-пакетов и ICMP-сообщения о невозможности фрагментации при установленном флаге DF.

В результате выполнения лабораторной работы студент должен уметь:

1. собирать линейную маршрутизируемую топологию из трёх маршрутизаторов;
2. настраивать статические маршруты;
3. объяснять принцип longest prefix match;
4. анализировать поле TTL в транзитном пакете;
5. изменять MTU на интерфейсе;
6. получать фрагментацию большого IP-пакета;
7. интерпретировать флаги MF, DF и Fragment Offset;
8. находить ICMP-сообщение типа `fragmentation needed`.

## Chapter I

### Рекомендации к работе

Как работать с лабораторной:

- Перед началом работы составь полную таблицу адресации по всем линкам.
- Сначала добейся обычной связности, и только потом переходи к TTL и фрагментации.
- Изменение MTU выполняй осознанно и фиксируй, на каком именно линке оно сделано.
- Для DF и ICMP-ошибки выбирай такую точку захвата, где ошибка реально будет видна.
- Все выводы делай по реальным полям пакета, а не по общим догадкам.

Как оформлять результат:

- Отчёт должен читаться как цельный документ.
- Термины `TTL`, `MTU`, `фрагментация`, `MF`, `DF`, `Fragment Offset`, `longest prefix match`, `статический маршрут` следует использовать корректно и по смыслу.

## Chapter II

### Теоретические основы лабораторной работы

#### Статическая маршрутизация

Маршрутизатор принимает решение о пересылке пакета на основании таблицы маршрутов. Таблица маршрутов содержит записи, каждая из которых указывает: для какой сети (адрес назначения и маска) через какой шлюз (next hop) или через какой интерфейс следует пересылать пакет.

В статической маршрутизации все нужные записи добавляются вручную. Это отличается от динамической маршрутизации, где маршруты обновляются автоматически протоколами вроде OSPF или BGP.

Для успешной маршрутизации необходимо:

1. чтобы на каждом маршрутизаторе был маршрут к сети назначения (или маршрут по умолчанию);
2. чтобы на всех промежуточных маршрутизаторах был включён IP forwarding;
3. чтобы обратный маршрут также существовал (пакет должен вернуться).

#### Longest prefix match

Если для одного и того же адреса назначения в таблице маршрутов подходят несколько записей, выбирается запись с самым длинным префиксом, то есть наиболее специфичный маршрут.

Например, если в таблице одновременно присутствуют:

- `10.20.0.0/16` через `10.0.12.2`
- `10.20.20.0/24` через `10.0.12.2`

то для адреса `10.20.20.10` будет выбран маршрут `/24`, поскольку он является более специфичным. Если маршрут `/24` удалить, тот же адрес будет направлен по маршруту `/16`.

Это правило гарантирует, что более точные маршруты всегда имеют приоритет над более общими.

#### Поле TTL

TTL (`Time to Live`) — это счётчик переходов пакета через маршрутизаторы. На каждом маршрутизаторе значение TTL уменьшается на единицу. Когда TTL достигает нуля, пакет отбрасывается, а отправителю отправляется ICMP-сообщение `Time Exceeded`.

TTL выполняет две функции:

1. **защита от петель маршрутизации** — даже при наличии петли пакет не будет циркулировать бесконечно;
2. **диагностика** — по значению TTL можно определить, через сколько маршрутизаторов прошёл пакет.

Например, если исходный TTL равен 64 (типичное значение для Linux), а в захваченном на транзитном линке пакете TTL равен 62, это означает, что пакет прошёл через 2 маршрутизатора.

#### MTU и фрагментация

MTU (`Maximum Transmission Unit`) — это максимальный размер пакета, который может быть передан через данный линк без фрагментации. Если размер IP-пакета превышает MTU следующего линка, пакет может быть разбит на несколько фрагментов.

При фрагментации в заголовке IP используются:

- **флаг `MF`** (`More Fragments`) — указывает, что за данным фрагментом следуют ещё фрагменты. Устанавливается в 1 для всех фрагментов, кроме последнего;
- **поле `Fragment Offset`** — указывает смещение данного фрагмента относительно начала исходного пакета, измеряется в единицах по 8 байт.

Фрагменты собираются обратно только на конечном узле-получателе. Промежуточные маршрутизаторы пересылают фрагменты независимо.

#### Флаг DF и ICMP-ошибка

Если на пакете установлен флаг **DF** (`Don't Fragment`), а MTU недостаточен для пересылки целого пакета, маршрутизатор не имеет права фрагментировать пакет. Вместо этого он:

1. отбрасывает пакет;
2. отправляет отправителю ICMP-сообщение `Destination Unreachable` с кодом `Fragmentation Needed` (тип 3, код 4);
3. в этом ICMP-сообщении указывает MTU следующего линка, что позволяет отправителю уменьшить размер пакетов.

Этот механизм лежит в основе **Path MTU Discovery** — алгоритма определения максимального размера пакета, который может быть доставлен до получателя без фрагментации.

#### Что необходимо понять по итогам теории

После изучения теоретического материала студент должен понимать:

1. как работает статическая маршрутизация;
2. почему маршрут `/24` имеет приоритет над `/16`;
3. зачем нужно поле TTL и как оно изменяется;
4. что такое MTU и когда возникает фрагментация;
5. что означают флаги MF и DF;
6. почему маршрутизатор отправляет ICMP `fragmentation needed`.

### Общие сведения о GNS3 и Linux-узлах

#### Среда GNS3

В лабораторной работе используются:

- `PC1`, `PC2` — Linux-узлы;
- `R1`, `R2`, `R3` — Linux-узлы с двумя сетевыми интерфейсами каждый.

Такая линейная топология удобна для демонстрации:

1. маршрутизации по нескольким переходам;
2. уменьшения TTL;
3. работы наиболее специфичного маршрута;
4. фрагментации на отдельном линке.

#### Команды настройки сети и маршрутизации

Для настройки IP-адресов и маршрутизации на Linux-узлах используются следующие команды:

| Команда | Назначение |
|---|---|
| `ip addr add <IP>/<маска> dev <интерфейс>` | назначить IP-адрес |
| `ip link set <интерфейс> up` | поднять интерфейс |
| `ip route add <сеть>/<маска> via <шлюз>` | добавить статический маршрут |
| `ip route del <сеть>/<маска>` | удалить маршрут |
| `ip route get <IP-адрес>` | показать, какой маршрут будет выбран |
| `ip route show` | показать таблицу маршрутов |
| `sysctl -w net.ipv4.ip_forward=1` | включить IP forwarding |
| `ip link set <интерфейс> mtu <значение>` | изменить MTU |
| `ping -s <размер> <IP-адрес>` | отправить ping с заданным размером payload |
| `ping -M do -s <размер> <IP-адрес>` | отправить ping с установленным флагом DF |

### Общие сведения о Wireshark

В данной лабораторной работе целесообразно использовать два типа захвата:

1. захват на линке `R2 — R3` для анализа TTL и обычной фрагментации;
2. захват рядом с маршрутизатором, формирующим ICMP-ошибку, для анализа `fragmentation needed`.

Полезные фильтры отображения для данной лабораторной работы:

| Фильтр | Назначение |
|---|---|
| `icmp` | показать ICMP-пакеты |
| `ip.flags.mf == 1 \|\| ip.frag_offset > 0` | показать фрагменты IP-пакетов |
| `icmp.type == 3 && icmp.code == 4` | показать ICMP `fragmentation needed` |

### Полезные материалы

В каталоге лабораторной находятся:

- `README.md` - основной текст лабораторной;
- `report.docx` - версия в формате Word;
- `report.pdf` - версия в формате PDF.

## Chapter III

### Общие требования к отчётам

Требования к отчётам, сформулированные в лабораторной работе № 1, применяются ко всем отчётам данного цикла. Ниже приведены дополнительные требования, специфичные для данной лабораторной работы.

В отчёте должны быть:

- титульный лист;
- цель работы;
- краткая теория по теме лабораторной;
- схема стенда;
- таблица адресации;
- выводы `ip route` для маршрутизаторов;
- результаты `ip route get`;
- скриншот успешного `ping PC1 → PC2`;
- скриншоты TTL, фрагментов и ICMP-ошибки;
- краткий вывод.

Дополнительно необходимо учитывать следующее:

- скриншоты Wireshark должны содержать список пакетов, дерево полей и байтовое представление;
- на скриншотах фрагментов должны быть видны флаг MF и Fragment Offset;
- в приложениях к отчёту должны присутствовать проект GNS3, файлы захвата и текст команд настройки маршрутов.

## Chapter IV

### Задание 1. Сборка стенда и настройка маршрутизации

Для этого задания используется параметр `A`, который вычисляется по формуле:

- `A = ((N - 1) mod 4) + 1`

Вариант адресации по параметру `A`:

| `A` | LAN1 | R1—R2 | R2—R3 | LAN2 |
|---|---|---|---|---|
| 1 | `10.0.1.0/24` | `10.0.12.0/24` | `10.0.23.0/24` | `10.20.20.0/24` |
| 2 | `172.16.1.0/24` | `172.16.12.0/24` | `172.16.23.0/24` | `172.16.20.0/24` |
| 3 | `192.168.1.0/24` | `192.168.12.0/24` | `192.168.23.0/24` | `192.168.20.0/24` |
| 4 | `10.10.1.0/24` | `10.10.12.0/24` | `10.10.23.0/24` | `10.10.20.0/24` |

Адресация определяется параметром `A`. Во всех вариантах используется одно и то же правило:

- в `LAN1` адрес маршрутизатора `R1` равен `.1`, адрес `PC1` равен `.10`;
- на линке `R1 — R2` маршрутизатор `R1` получает адрес `.1`, маршрутизатор `R2` получает адрес `.2`;
- на линке `R2 — R3` маршрутизатор `R2` получает адрес `.2`, маршрутизатор `R3` получает адрес `.3`;
- в `LAN2` адрес маршрутизатора `R3` равен `.1`, адрес `PC2` равен `.10`.

Полная таблица адресации для всех узлов:

| `A` | `PC1 eth0` | `R1 eth0` | `R1 eth1` | `R2 eth0` | `R2 eth1` | `R3 eth0` | `R3 eth1` | `PC2 eth0` |
|---|---|---|---|---|---|---|---|---|
| 1 | `10.0.1.10/24` | `10.0.1.1/24` | `10.0.12.1/24` | `10.0.12.2/24` | `10.0.23.2/24` | `10.0.23.3/24` | `10.20.20.1/24` | `10.20.20.10/24` |
| 2 | `172.16.1.10/24` | `172.16.1.1/24` | `172.16.12.1/24` | `172.16.12.2/24` | `172.16.23.2/24` | `172.16.23.3/24` | `172.16.20.1/24` | `172.16.20.10/24` |
| 3 | `192.168.1.10/24` | `192.168.1.1/24` | `192.168.12.1/24` | `192.168.12.2/24` | `192.168.23.2/24` | `192.168.23.3/24` | `192.168.20.1/24` | `192.168.20.10/24` |
| 4 | `10.10.1.10/24` | `10.10.1.1/24` | `10.10.12.1/24` | `10.10.12.2/24` | `10.10.23.2/24` | `10.10.23.3/24` | `10.10.20.1/24` | `10.10.20.10/24` |

Шлюзы по умолчанию:

- для `PC1` — адрес `R1 eth0`;
- для `PC2` — адрес `R3 eth1`.

Собрать линейную топологию: `PC1 — R1 — R2 — R3 — PC2`.

Порядок настройки маршрутизации:

На `R1`, `R2`, `R3` включить IP forwarding.

Статические маршруты (на примере варианта `A = 1`):

- На `R1`: маршрут `10.20.0.0/16` через `R1—R2` адрес `.2` и маршрут `10.20.20.0/24` через тот же адрес.
- На `R2`: маршрут к LAN1 через `R1—R2` адрес `.1` и маршрут к LAN2 через `R2—R3` адрес `.3`.
- На `R3`: маршрут к LAN1 через `R2—R3` адрес `.2`.

На `R1` должны быть созданы как минимум два маршрута: более общий (`/16`) и более специфичный (`/24`), чтобы продемонстрировать longest prefix match.

В отчёт по данному заданию включить:

- схему стенда;
- таблицу адресации;
- команды настройки маршрутов.

В составе сдаваемых материалов должны быть представлены:

- проект GNS3;
- текстовые команды настройки маршрутов.

### Задание 2. Проверка связности и таблицы маршрутов

Для этого задания используется параметр `B`, который вычисляется по формуле:

- `B = ((N - 1) mod 2) + 1`

Вариант проверки по параметру `B`:

| `B` | Направление проверки |
|---|---|
| 1 | `PC1 → PC2` |
| 2 | `PC2 → PC1` |

1. выполнить `ping` в соответствии с параметром `B`;
2. убедиться, что связность работает;
3. включить в отчёт текст или скриншот результата.

Дополнительно в отчёт включить:

1. вывод `ip route` на `R1`;
2. вывод `ip route` на `R2`;
3. вывод `ip route` на `R3`.

### Задание 3. Демонстрация longest prefix match

Для этого задания используется параметр `C`, который вычисляется по формуле:

- `C = ((N - 1) mod 3) + 1`

Вариант демонстрации по параметру `C`:

| `C` | Действие |
|---|---|
| 1 | Показать выбор маршрута `/24`, удалить `/24`, показать выбор `/16`, вернуть `/24` |
| 2 | Показать выбор маршрута `/24`, удалить `/24`, показать что маршрут `/16` также работает, вернуть `/24` |
| 3 | Показать оба маршрута в таблице, удалить `/24`, показать отсутствие `/24`, вернуть `/24`, подтвердить восстановление |

На `R1` выполнить демонстрацию longest prefix match в соответствии с параметром `C`:

1. выполнить `ip route get <IP-адрес PC2 из своего варианта>` при наличии маршрутов `/16` и `/24` и зафиксировать, что выбран маршрут `/24`;
2. удалить маршрут `/24`;
3. снова выполнить `ip route get <IP-адрес PC2 из своего варианта>` и зафиксировать, что теперь используется маршрут `/16`;
4. вернуть маршрут `/24`.

В отчёте необходимо показать:

1. что сначала выбирается маршрут `/24`;
2. что после удаления `/24` используется маршрут `/16`;
3. объяснение, почему так происходит.

### Задание 4. Демонстрация TTL

Для этого задания используется параметр `D`, который вычисляется по формуле:

- `D = ((N - 1) mod 2) + 1`

Вариант точки захвата по параметру `D`:

| `D` | Линк захвата | Сколько маршрутизаторов прошёл пакет |
|---|---|---|
| 1 | `R2 — R3` | 2 |
| 2 | `R1 — R2` | 1 |

1. запустить захват на линке, определяемом параметром `D`;
2. выполнить `ping` `PC1 → PC2`;
3. выбрать ICMP Echo Request в транзите;
4. показать значение TTL в этом пакете.

Необходимо письменно объяснить, почему TTL меньше исходного значения, которое обычно формирует конечный узел (например, 64).

В отчёт по данному заданию включить:

- скриншот ICMP Echo Request с полем TTL;
- значение TTL и расчёт числа пройденных маршрутизаторов;
- пояснение.

### Задание 5. Демонстрация фрагментации

Для этого задания используется параметр `E`, который вычисляется по формуле:

- `E = ((N - 1) mod 3) + 1`

Вариант размера MTU и payload по параметру `E`:

| `E` | MTU на линке `R2 — R3` | Размер payload `ping` |
|---|---|---|
| 1 | `600` | `2000` |
| 2 | `800` | `3000` |
| 3 | `500` | `1500` |

1. установить на интерфейсах линка `R2 — R3` уменьшенный MTU в соответствии с параметром `E`;
2. запустить захват на линке `R2 — R3`;
3. выполнить с `PC1` команду `ping` с размером полезной нагрузки из параметра `E` без установки DF: `ping -s <размер из параметра E> <IP-адрес PC2 из своего варианта>`;
4. остановить захват;
5. сохранить файл под именем `pcap/lr7_fragments.pcapng`.

В Wireshark необходимо:

1. найти несколько фрагментов одного IP-пакета;
2. показать флаг `MF` (`More Fragments`);
3. показать `Fragment Offset`;
4. объяснить, почему пакет был разбит.

В отчёт по данному заданию включить:

- скриншоты фрагментов в Wireshark;
- значения MF и Fragment Offset;
- пояснение причины фрагментации.

В составе сдаваемых материалов должен быть представлен:

- файл `pcap/lr7_fragments.pcapng`.

### Задание 6. Демонстрация DF и ICMP-ошибки

Для этого задания используется параметр `F`, который вычисляется по формуле:

- `F = ((N - 1) mod 3) + 1`

Вариант аналитического пункта по параметру `F`:

| `F` | Аналитический пункт |
|---|---|
| 1 | Объяснить, почему при DF пакет не был фрагментирован и кто именно из маршрутизаторов сообщил об ошибке |
| 2 | Показать, как MTU следующего линка связан с ICMP-сообщением `fragmentation needed` |
| 3 | Сравнить фрагментированную передачу из предыдущего задания с передачей при DF: в чём принципиальное различие |

1. запустить отдельный захват на линке рядом с маршрутизатором, который должен сформировать ICMP-ошибку;
2. выполнить с `PC1` команду `ping` с большим размером полезной нагрузки и установленным флагом DF: `ping -M do -s <размер из параметра E> <IP-адрес PC2 из своего варианта>` (размер взять из параметра `E`);
3. найти ICMP-сообщение типа `Destination Unreachable` с кодом `fragmentation needed` (ICMP type 3, code 4);
4. остановить захват.

В отчёте необходимо:

1. пояснить, почему при DF пакет не был фрагментирован;
2. выполнить аналитический пункт по параметру `F`.

В отчёт по данному заданию включить:

- скриншот ICMP-сообщения об ошибке;
- выполнение аналитического пункта по параметру `F`.
